@{
    var name = Request["name"];
    var db = Database.Open("card_db");
    var query = "SELECT * FROM card WHERE name LIKE '%" + name + "%'";
    var data = db.Query(query); 
    var grid = new WebGrid(data);
}
@grid.GetHtml()